# KubeJS Example Scripts

These files are copies of the KubeJS example scripts shipped with this workspace.

- `docs/kubejs_examples/` is for reference/documentation.
- The scripts that actually run in-game must stay under `kubejs/*_scripts/`.
